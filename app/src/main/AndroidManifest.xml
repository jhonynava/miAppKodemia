<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.kodemia.miappkodemia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiAppKodemia">
        <activity android:name=".MyRecyclerView"></activity>
        <activity
            android:name=".Settings"
            android:parentActivityName=".FormularioLinearLayout" />
        <activity android:name=".FormularioConstraint" />
        <activity android:name=".FormularioLinearLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MiActivity2" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>